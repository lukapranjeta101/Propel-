Propel-Tax-Advisory

Project structure (skeleton):

- `index.html` — top-level HTML entry (empty)
- `assets/css/styles.css` — stylesheet (empty)
- `assets/js/main.js` — JavaScript entry (empty)
- `assets/img/` — images directory (contains `.gitkeep` to preserve folder)

Next steps: add starter HTML/CSS/JS when ready.